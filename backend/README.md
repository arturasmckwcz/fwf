# FWF API

FWF API is the GraphQL endpoint at .../api/graphql

The backend uses node/express for http requests and
Objection/knex/PostgreSQL for db.

## GraphQL

For GraphQL functionality express-graphql lib is utilised and JavaScript is used for schema.
