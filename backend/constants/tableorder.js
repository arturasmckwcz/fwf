const tablenames = require('./tablenames');

module.exports = [
  tablenames.role,
  tablenames.user,
  tablenames.production,
  tablenames.prescription,
  tablenames.blood,
  tablenames.patient,
  tablenames.doctor,
  tablenames.clinic,
  tablenames.lysate,
  tablenames.product,
  tablenames.person
];